x-swagger-router-controller: filter
post:
  summary: Filter property
  description: Filter property
  parameters:
  - name: body
    description: body
    in: body
    required: true
    schema:
      $ref: "#/definitions/PropertyRequest"
  responses:
    200:
      description: Returns list of filtered properties
      schema:
        $ref: '#/definitions/PropertyResponse'
    400:
      $ref: '#/responses/400'
    401:
      $ref: '#/responses/401'
    404:
      $ref: '#/responses/404'
    500:
      $ref: '#/responses/500'
